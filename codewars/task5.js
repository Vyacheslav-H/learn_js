// Create a function that takes an integer as an argument
// and returns "Even" for even numbers or "Odd" for odd numbers.
function EvenOrOdd(number) {
  if (i % 2 === 0) {
    return 'Even';
  }
  return 'Odd';
}

// да-да... я не могу привыкнуть к использованию тернарных операторов)
